<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
          display: flex;
          flex-direction: column;
          min-height: 100vh;
        }
        main {
          flex: 1;
        }
      </style>

    <title>Nuevo Superheroe</title>
</head>

<body>
    <main class="container">  
        <%- include('partials/header.ejs') -%>
        <br>

    <!-- Mostrar errores si existen -->
    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
        <ul style="color: red;">
            <% errors.forEach(error => { %>
                <li><%= error %></li>
            <% }) %>
        </ul>
    <% } %>

    <% if (superheroe.id) { %>
        <h1>Editar Superheroe</h1>        
        <form action="/api/heroes/<%= superheroe.id %>?_method=PUT" method="POST">
    <% } else { %>
        <h1>Nuevo Superhéroe</h1>
        <form action="/api/heroes" method="POST">
    <% } %>


    <!-- Formulario para agregar un superhéroe -->
    
    <div class="mb-3">
        <label for="nombreSuperHeroe" class="form-label">Nombre del Superhéroe:</label>
        <input type="text" class="form-control" name="nombreSuperHeroe" id="nombreSuperHeroe" 
               value="<%= superheroe.nombreSuperHeroe || '' %>" required>
      </div>
    
      <div class="mb-3">
        <label for="nombreReal" class="form-label">Nombre Real:</label>
        <input type="text" class="form-control" name="nombreReal" id="nombreReal" 
               value="<%= superheroe.nombreReal || '' %>" required>
      </div>
    
      <div class="mb-3">
        <label for="edad" class="form-label">Edad:</label>
        <input type="number" class="form-control" name="edad" id="edad" 
               value="<%= superheroe.edad || '' %>" required>
      </div>
    
      <div class="mb-3">
        <label for="poderes" class="form-label">Poderes (separados por comas):</label>
        <input type="text" class="form-control" name="poderes" id="poderes" 
               value="<%= superheroe.poderes ? superheroe.poderes.join(', ') : '' %>" required>
      </div>
    
      <div class="mb-3">
        <label for="aliados" class="form-label">Aliados (separados por comas):</label>
        <input type="text" class="form-control" name="aliados" id="aliados" 
               value="<%= superheroe.aliados ? superheroe.aliados.join(', ') : '' %>">
      </div>
    
      <div class="mb-3">
        <label for="enemigos" class="form-label">Enemigos (separados por comas):</label>
        <input type="text" class="form-control" name="enemigos" id="enemigos" 
               value="<%= superheroe.enemigos ? superheroe.enemigos.join(', ') : '' %>">
      </div>
    
      <div class="mb-3">
        <% if (superheroe.id) { %>
          <button type="submit" class="btn btn-warning">Actualizar Superhéroe</button>
        <% } else { %>
          <button type="submit" class="btn btn-success">Agregar Superhéroe</button>
        <% } %>
      </div>

        
    </form>
</main>

<%- include('partials/footer.ejs') -%>
</body>
</html>
