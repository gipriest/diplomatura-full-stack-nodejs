<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Vista Layout desde NodeJS</title>

    <style>
        body {
          display: flex;
          flex-direction: column;
          min-height: 100vh;
        }
        main {
          flex: 1;
        }
      </style>

</head>
<body>

    <main class="container">

        <%- include('partials/header.ejs') -%>

        <% if (superheroe && superheroe.edad) { %>

            <br>
            <div class="card">
                <h5 class="card-header">¿Desea eliminar?</h5>
                <div class="card-body">
                  <h5 class="card-title"><%= superheroe.nombreReal %></h5>
        
                  <form action="/api/heroes/id/<%= superheroe.id %>?_method=DELETE" method="POST">
                    <button type="submit" class="btn btn-success">Si, Eliminar</button>
                </form>
                </div>
            </div>

        <% } else { %>
            <p style="color: red;">No se encontró información del superhéroe.</p>
        <% } %>

    

</main>
    
<%- include('partials/footer.ejs') -%>
</body>
</html>